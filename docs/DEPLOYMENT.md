# Deployment

Daily schedule is set to UTC `02:07`, which equals Beijing time `10:07` (chosen to avoid top-of-hour scheduler congestion).

Workflows:
- `.github/workflows/daily_update.yml`
- `.github/workflows/deploy_site.yml`
